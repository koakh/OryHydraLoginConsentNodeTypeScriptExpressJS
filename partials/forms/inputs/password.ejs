<%- include('../input-text', {
  fieldType:'password',
  fieldId:'password',
  fieldLabel:'password',
  fieldPlaceholder:'passwordPlaceholder',
  fieldValue: typeof data !== 'undefined' && typeof data.password !== 'undefined' ? data.password: '',
  validationErrorMessage: typeof validationErrors !=='undefined' && typeof validationErrors.password !=='undefined' ? validationErrors.password.msg : undefined
}) %>
